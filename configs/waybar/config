```json
{
    // General Settings
    "layer": "top",
    "margin-bottom": 0,
    "margin-left": 0,
    "margin-right": 0,
    "spacing": 0,

    // NOTE: prefer a local modules.jsonc shipped in configs/waybar to avoid missing-file errors
    // If you prefer your home config, change this include back to "~/.config/waybar/modules.jsonc"
    "include": ["modules.jsonc"],

    // Modules Left
    "modules-left": [
        "custom/appmenuicon",
        "hyprland/workspaces"
    ],

    // Modules Center
    "modules-center": [
        "hyprland/window"
    ],

    // Modules Right
    "modules-right": [
        "custom/updates",
        "pulseaudio",
        "battery",
        "hyprland/language",
        "group/hardware",
        "custom/cliphist",
        "custom/exit",
        "clock",
        "tray"
    ]
}
```
